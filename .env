# API Configuration
VITE_API_URL=http://localhost:5000/api

# MongoDB Configuration (for reference only - backend uses its own .env)
VITE_MONGODB_URI=mongodb://localhost:27017
VITE_MONGODB_DB=javaprep_arena
